// app/layout.jsx (or a nested route layout like app/dashboard/layout.jsx)

import React from "react";

const Layout = ({ children }) => {
  return <div>{children}</div>;
};

export default Layout;
    